<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.myapp.dao.daoVO.UserExtDAO">
    <resultMap id="BaseResultMap" type="com.example.myapp.model.User">
        <id column="open_id" jdbcType="VARCHAR" property="openId" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="avatar" jdbcType="VARCHAR" property="avatar" />
        <result column="expirationtTime" jdbcType="TIMESTAMP" property="expirationttime" />
        <result column="token" jdbcType="VARCHAR" property="token" />
    </resultMap>
    <sql id="Base_Column_List">
        open_id, username, avatar, expirationtTime, token
    </sql>
    <select id="selectAll" resultMap="com.example.myapp.dao.daoVO.UserExtDAO.BaseResultMap" parameterType="com.example.myapp.model.User">
        select
        <include refid="Base_Column_List"></include>
        from user
    </select>
    <insert id="insertUser" keyColumn="open_id" keyProperty="openId" parameterType="com.example.myapp.model.User" useGeneratedKeys="true">
        insert into user (username, avatar,open_id,expirationtTime,token)
        values (#{username,jdbcType=VARCHAR}, #{avatar,jdbcType=VARCHAR},#{openId,jdbcType=VARCHAR},#{expirationttime,jdbcType=TIMESTAMP},#{token,jdbcType=VARCHAR})
    </insert>
    <select id="selectByToken" resultMap="com.example.myapp.dao.daoVO.UserExtDAO.BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List"></include>
        from user where token = #{token,jdbcType=VARCHAR}
    </select>
    <update id="updateExpirationtTime" parameterType="com.example.myapp.model.User">
        update user
        set expirationtTime = #{expirationttime,jdbcType=TIMESTAMP},
            token = #{token,jdbcType=VARCHAR}
        where open_id = #{openId,jdbcType=VARCHAR}
    </update>
</mapper>